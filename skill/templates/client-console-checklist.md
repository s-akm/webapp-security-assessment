<!--
評価者へ（渡す前にこのコメントを消す）
- 使っていないサービスの節は、番号ごと消してから渡す。残した節は「該当なし」と書ける形のままにする
- 対応する資料: 1 は references/03-runtime-verification.md の 1 節・3 節・4 節・6 節、2 は 02 の E-1 と 07 の 11-4、
  3 は 03 の 7 節、4 は 03 の 3 節、5 は 02 の F-4 と 03 の 3 節、6 は 02 の F-5 と 06 の「カード決済を扱う場合」、
  7 は 10 の 3-3、8 は 08 の 1-2
- 返ってきた結果は台帳に戻す（references/04-findings-register.md の「依頼者の確認の結果を戻す」）
-->

# 管理画面の設定の確認のお願い

このシートは、**各サービスの管理画面にある設定**を確認するためのものです。
評価者が管理画面のアカウントをお預かりすることは避けています。お手元で画面を開き、
**表示されている項目名と値を、そのまま書き写して**ください。

- 所要時間の目安: 30〜45 分（使っているサービスの数による）
- **判断は不要です。** 「良いか悪いか」ではなく、**画面に出ている項目名と値**をご記入ください
- 画面の名前や場所は、サービス側の変更で変わることがあります。見つからなければ「見つからなかった」とお書きください

---

## 記入欄（最初に）

```
サービス名:
対象の URL:
実施日時:
使ったアカウントの種類（例: 組織の管理者 / 開発者 / 閲覧のみ）:
```

---

## ⚠️ 作業中のお願い

- **設定を変えないでください。** スイッチやボタンには触れず、見るだけにしてください。開いた画面は閉じるだけで構いません
- **鍵・トークン・パスワード・接続文字列の値は、書き写さないでください。** 書いていただくのは、**名前・種類・有効か無効か・作成日・最後に使われた日**までです
- メンバーの一覧は、**人数と役割の内訳**だけで構いません。お名前やメールアドレスは不要です
- 画面の写真を撮っていただける場合は、鍵の値や個人の情報の部分を隠してからお願いします

---

## 1. Supabase（使っている場合）

**1-1. Security Advisor**（Advisors → Security Advisor）

```
一覧に出ている行を、そのまま書き写してください（項目名 / 対象 / レベル）:


□ 一覧は空だった
```

**1-2. Realtime の設定**（Realtime → Settings）

```
「Allow public access」（名前が近い項目でも可）の状態:  □ オン　□ オフ　□ 見つからなかった
```

**1-3. API に公開しているスキーマ**（Settings → Data API など）

```
Exposed schemas の欄に並んでいる名前:
```

**1-4. API の鍵**（Settings → API Keys）

```
新しい方式の鍵（publishable / secret）の名前と、最後に使われた日:

旧方式の鍵（anon / service_role）のタブで表示されている状態
（例: 「無効化する」ボタンがある / 「無効」と表示されている）:
```

**1-5. 認証の設定**（Authentication → Sign In / Providers、Policies など）

```
新規登録を許可する設定:                □ オン　□ オフ
匿名でのサインイン:                    □ オン　□ オフ
パスワードの最小の長さ:                　　文字
漏えいしたパスワードの検知:            □ オン　□ オフ　□ 上位のプランでのみ利用可能と表示
多要素認証の方式で有効になっているもの:
SMS の送信元（使っている場合）:
```

**1-6. バックアップ**（Database → Backups）

```
表示されている内容（例: 日次で 7 日分 / 特定の時点への復元が可能 / 上位のプランでのみ利用可能）:
```

---

## 2. Firebase（使っている場合）

**2-1. ルール**（Firestore・Realtime Database・Storage それぞれの「ルール」タブ）

```
ルールの本文（そのまま貼り付け。鍵は含まれていません）:

最後に公開された日時:
```

**2-2. App Check**（App Check → API）

```
サービスごとの状態（例: Cloud Firestore: 適用 / 未適用）:
```

**2-3. 電話番号による認証**（Authentication → 設定）

```
SMS を送ってよい地域の設定（許可している国の一覧、または「すべて」）:
SMS の不正対策（reCAPTCHA）の設定で表示されている内容:
```

---

## 3. Vercel（使っている場合）

**3-1. デプロイの保護**（Project → Settings → Deployment Protection）

```
Vercel Authentication の対象（例: 本番以外すべて / 本番を含むすべて / 無効）:
パスワードでの保護:                           □ 有効　□ 無効
自動化用の迂回（Protection Bypass for Automation）: □ 設定あり　□ なし
共有用のリンク（Shareable Links）:             □ 発行済みのものがある　□ ない
「Legacy」と付いた項目があれば、その名前と状態:
```

**3-2. 環境変数**（Project → Settings → Environment Variables）

```
変数ごとに、名前 / 種類（Config・Secret・Sensitive など、表示どおり） / 対象の環境（Production・Preview・Development）:
（値は書き写さないでください）

```

**3-3. チームの方針**（Team → Settings → Security など）

```
本番の Secret の値を他の環境と分ける方針（Separate Production Secret Values など）: □ 有効　□ 無効　□ 見つからなかった
```

---

## 4. 管理画面そのものへのログインの守り

ホスティング、データベース、認証、ドメインの登録、メール配信、GitHub、決済の**それぞれ**について書いてください。

```
サービス名 / 組織として多要素認証を必須にしているか（□ 必須 □ 任意 □ 設定が無い）/ メンバーの人数と役割の内訳 / 心当たりの無いアカウントがあるか（□ ある □ ない）

-
-
-
```

---

## 5. SMS の送信（SMS で確認コードや通知を送っている場合）

送っている基盤（例: Twilio、AWS、Firebase）の管理画面で確認してください。

```
基盤の名前:
送信を許可している国（一覧、または「すべて」）:
月の送信数や金額の上限の設定と、その値:
上限に近づいたときの通知の設定:          □ ある（宛先の種類: 　　　）　□ ない
直近 30 日の送信数（画面に出ていれば）:
確認コードの完了率（画面に出ていれば）:
```

---

## 6. カード決済（カード決済を扱っている場合）

決済代行（例: Stripe）の管理画面で確認してください。

```
不正検知の機能（例: Radar）:        □ 有効　□ 無効　□ 見つからなかった
不正検知のルールの一覧（名前と動作。例: 「リスクが高い支払いをブロック」）:

3-D セキュアを求める設定（表示どおり）:
EMV 3-D セキュアの利用状況が分かる表示があれば、その内容:
```

---

## 7. GitHub の組織の設定（GitHub を使っている場合）

組織の Settings → Actions → General と、Settings → Authentication security で確認してください。

```
利用を許可している Action の範囲（表示どおり）:
Action をコミットのハッシュで固定することを求める設定: □ オン　□ オフ　□ 見つからなかった
ワークフローの既定の権限（Read / Read and write）:
ワークフローが Pull Request を作成・承認することの許可: □ オン　□ オフ
フォークからの Pull Request でワークフローを動かすときの承認の設定（表示どおり）:
組織のメンバーに二要素認証を必須にする設定: □ オン　□ オフ
```

---

## 8. 画面操作を記録するツール（セッションリプレイを使っている場合）

ツール（例: Sentry、Microsoft Clarity、Hotjar、LogRocket）の管理画面で確認してください。

```
ツールの名前:
マスク（伏せ字）の設定の項目名と値（例: 文字のマスク: すべて / 入力欄のマスク: オン）:
録画の保持期間:
IP アドレスの記録:                    □ する　□ しない　□ 見つからなかった
管理画面で録画を見られるメンバーの人数:
```

---

## ご返送について

- **記入したこのシート**をお送りください
- 見つからなかった項目、権限が無く開けなかった画面は、そのままお書きください。
  **未確認であることが分かるほうが、確認したつもりで進むより安全です**
- ご不明な点があれば、途中で止めてご連絡ください
