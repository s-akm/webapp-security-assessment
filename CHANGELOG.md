# 変更履歴

スキルに何をしたかの記録。**きっかけになった案件を必ず書く。** 何を直したかだけが残っていても、
その判断が正しかったかを後から確かめられない。

版は `VERSION` にあり、配布物のファイル名に付く。

---

## [2.14.0] — 2026-09-18

**リポジトリを公開できる形にした回。** スキル本体（`skill/`）は変えていない。

### 変更

- **適用事例（`cases/`）をリポジトリから外した。** 依頼者の未修正の脆弱性が再現できる粒度で
  書かれているため。手元には残し、`.gitignore` で除外する。**git の履歴も作り直した**
  （HEAD から消しても、履歴に残っていれば取り出せる）
- **案件を示す語を `tests/run.sh` から `tests/ngwords.local`（`.gitignore` 済み）へ移した。**
  依頼者を示す語そのものが機微で、検査のためにリポジトリへ書いては本末転倒だった。
  手元ファイルが無い環境では、検査がその旨を出す。検査の検査が混入させる語は、手元ファイルに
  依らず必ず捕まる語（`CLIENT-NGWORD-CANARY`）にした
- CHANGELOG の過去の記述から、案件名・案件のディレクトリ・業務上の関数名・指摘 ID を落とした。
  気づいたこと（教訓）はそのまま残している
- LICENSE（MIT）を置いた

### 教訓

**「配布物に混ざらない」と「リポジトリに置いてよい」は別。** `skill/` の混入検査は初版から
あったが、検査のために書いた案件語と、教材として置いた事例は、リポジトリを公開した瞬間に
そのまま漏れる。公開の可否は配布物ではなくリポジトリ全体（履歴を含む）で見る。

---

## [2.13.0] — 2026-09-11

**残っていた課題のうち、`recon.sh` の DNS まわりを検査できるようにした回。**

### 追加

- **`recon.sh` に `RECON_DNS`** — `サーバー:ポート` を入れると、そのサーバーに DNS を聞く。
  社内の権威サーバーに直接聞きたいとき、検査用の受け口に向けたいときに使う。
  未設定なら OS の設定どおり（通常はこちら）。`dig +short` 11 箇所を `dq()` に集約した
- **`tests/fixtures/site/dns.py`** — 決まった応答を返す DNS の受け口。標準ライブラリだけで
  RFC 1035 の応答を組み立てる。わざと穴のある構成（apex に SPF 無し、DMARC が `p=none`、
  CAA 無し、DNSSEC 未署名）に、配信サービス用サブドメインの SPF と DKIM セレクタを足してある
- **DNS の検査 8 件**（検査は 291 → 299 件）
  - **`localhost` は OS が特別扱いして常に `127.0.0.1` を返す**（RFC 6761）ため、
    DNS の検査には使えなかった。解決できない名前（`example.test`）で `recon.sh` を呼び、
    HTTP 側は即座に失敗させて DNS の節だけを見る

### 修正

- **DMARC の `rua` に入った連絡先アドレスが、出力にそのまま出ていた。** `recon.sh` は末尾で
  「報告書には不要」と注意していたが、出力自体には出ていた。末尾の注意書きだけでは、
  貼り付けたときに残る。**出力の時点で `mailto:<伏字>` にした**。
  この不具合は、DNS の検査を足した直後に「連絡先を出力に混ぜない」の検査が捕まえた

### 変更

- README の「直す」に、**節番号を変えるときの手順**（参照を全部洗う `grep`）を足した。
  2.12.0 の入れ替えで 3 か所に波及したが、検査は「入れ替え」を捕まえられない
- ミューテーションに「DMARC の連絡先を伏せない」を追加（20 → 21 件）

---

## [2.12.0] — 2026-09-11

**自己レビューで見つかった不整合を直した回。** 「他に反映すべき箇所は無いか」を、
相互参照・数値・カバレッジの 3 つの軸で機械的に洗った。

### 見つかったもの（8 件）

| 軸 | 何が | 直し方 |
|---|---|---|
| 相互参照 | `audit_grep.sh` の節番号が `0.` 重複、`2b → 2d → 2c` の順 | 「対象」を番号なしに、2c と 2d を入れ替え |
| 相互参照 | 入れ替えに伴い `02` の参照 2 箇所とテストの範囲指定が逆に | 修正（**テストが捕まえた**） |
| 数値 | README「スクリプト 4 本」→ 実態 5 本 | 修正 |
| 数値 | README「52 の枠組み」→ 実態 54 | 修正 |
| 完全性 | SKILL.md の「進め方」に 13 / 14 が無い（12 だけ足して忘れた） | 追加 |
| 完全性 | `04` に `--api` の記述が無い | 追加 |
| 完全性 | SKILL.md のスクリプト表に `--owasp` `--api` が無い | 追加 |
| カバレッジ | `--owasp 2021` / `--api` / full の 2 枚の**中身**が未検査（シート数しか見ていなかった） | 検査を追加（288 → 291 件） |

### 追加

- **自己監査に「README の数値と実態の突き合わせ」**。資料・スクリプト・題材・検査・変異の
  数を実測して README と比べる。**手で書いた数は必ずずれる**ので、機械に見させる。
  追加した直後に「検査 288 件」→ 291 のずれを捕まえた
- 適用事例の一覧に**使ったスキルの版**の列（事例はリポジトリの外に置いている）。事例の中の
  「N 節」のような参照はその版の番号で、スキル側で節番号が変わっても事例は直さない。
  読むときは版を見て CHANGELOG で追う
- `06` の ASVS 対応に Webhook（V4）
- `audit_grep.sh` 8 節のテーブル名抽出から、英文の断片（`auth` / `https` / `your` など）を除外

### 教訓

**節番号を変えると 3 か所に波及する**（資料の参照・テストの範囲指定・事例の記述）。
テストが 2 つ目を捕まえたが、資料の参照は自己レビューで目視して見つけた。
節番号への参照が実在するかを見る検査は入っているが、**「入れ替え」は両方実在するので通る**。
番号ではなく見出しの語で参照するほうが壊れにくい。

---

## [2.11.0] — 2026-09-11

**LLM Top 10 の 2026 版を PDF から反映し、検査の正当性を担保する仕組みを入れた回。**

### LLM Top 10 2026 版（PDF 122 ページを読んで反映）

2026-08-03 発行。**初めて 7,700 件超の実インシデントを投票と突き合わせた版**で、
「恐れ」と「実態」の差が明示された。`references/12-ai-features.md` に反映した。

| 変化 | 反映 |
|---|---|
| **Excessive Agency が 6 位 → 3 位**（「最も重大な移動」） | 3 節に「ツールを呼ぶ構成なら報告書の先頭に置く」 |
| **Unbounded Consumption が 10 位 → 6 位** | 5 節に追記 |
| **Misinformation** — 投票は下位、インシデントでは上位 | **5-2 節を新設。** 出力がツール呼び出しの引数や画面の判定結果になっていないか |
| System Prompt Leakage → **Hidden Context Exposure** に改称・拡張 | 4 節。システムプロンプトだけでなく、検索文書・ツール結果・記憶・他の利用者の断片も |
| Prompt Injection が**クロスモーダル**（画像・音声）を包含 | 1 節。画像を読ませる機能は「命令が入りうる入力」 |
| Improper Output Handling が**生成コード**を包含 | 2 節。生成したコードをサンドボックス無しで実行・出荷していないか |
| **境界の明示** | モデルが「アクター」（ツール・記憶・下流への影響）なら Agentic Top 10 の領域。**両方を見る** |

### 追加: 検査の検査（`tests/mutations.sh`）

**検査は「通ること」しか示さない。** これまで「わざと壊して落ちるか」を手で確かめてきたが、
それを 20 件のミューテーションとして自動化した。スキルに欠陥を入れて `run.sh` を回し、
対応する検査が落ちることを 1 つずつ見る。

**初回に回したところ、20 件中 5 件で問題が出た。**

- **検査が弱かった（3 件）**: 「日本語が壊れない」は先頭付近の文字を期待値にしていたため、
  行末で起きる文字化けを検出できていなかった → 出力全体が正しい UTF-8 かで判定するよう変えた。
  「読む資料を名指しする」は出力全体を探していたため、判定を外しても見出しの文字列に当たって
  通っていた → 「追加で読む資料:」のブロック内だけを見るよう変えた。「ユーティリティを
  ハンドラに数えない」は題材がそもそも収集されない形だったため、除外の仕組みを確かめて
  いなかった → 題材を収集パターンに当たる形にした
- **ミューテーションの不備（2 件）**: 復元に `cp` を使っていたためパーミッションが戻らず、
  次の検査を巻き込んで落とした → `cp -p` に

### 追加: 自己監査（`tests/self-audit.sh`）

「動く」「正しい」「配ってよい」を 4 段で見る。**初回に回したところ、スキル自身が自分の
方針に違反していた** — `references/07` に「〜なら要検討。」が残っていた。SKILL.md は
「中間の表現（やや懸念・要検討）を作らない」と書いている。判定と手順に置き換えた。

### 修正

- `tests/*.sh` で、変数展開の直後にマルチバイト文字が来る箇所を機械的に `${}` に
  （bash 3.2 の既知の挙動。以前も踏んだ）

### 起きた事故と、その対策

**`mutations.sh` を回している最中に、ビルドと導入済みスキルの同期を走らせてしまった。**
`mutations.sh` は実行中に `skill/` へ欠陥（案件語の混入など）を入れて戻す。その最中に
`skill/` を読む処理が走ると、欠陥入りの状態がそのまま配布物に入る。**実際に、導入済みの
スキルに案件語入りの SKILL.md がコピーされた**（監査完了後に再同期して除去済み）。

- `mutations.sh` は実行中 `tests/.mutating` を置き、終了時（中断時も）に消す
- `build.sh` と `self-audit.sh` は、ロックがあれば**止まる**
- **検査の仕組みが「検査対象を壊す」種類のものであるとき、同時実行の排他が要る。**
  これは検査を書いた時点で気づくべきことだった

---

## [2.10.0] — 2026-09-11

**参照している基準をすべて一次情報（発行元のサイト）で確認し直した回。**
二次情報の解説記事ではなく、owasp.org / cisecurity.org / ipa.go.jp / ppc.go.jp を直接見た。

### 一次情報で確認した版

| 基準 | 結果 |
|---|---|
| OWASP Top 10 | 2025 で変わらず。**A10 の公式定義に CWE-636（Failing Open）・CWE-209・CWE-274 が名指し**されていることを追記 |
| OWASP API Security Top 10 | **2023 が最新。** 新版の予告なし |
| OWASP ASVS | 5.0.0（2025-05-30）。**「約 350」ではなく 345 要件**に修正。**L1 は適合を証明せず、証明できるのは L2 / L3** という公式の位置づけを追記 |
| OWASP Top 10 for LLM | **2026 版が 2026-08-03 に出ていた。** 詳細は PDF のみで取れず、**差分は未反映と表に明記** |
| OWASP MASVS | 2.1.0（2024-01）で変わらず |
| **CIS Benchmarks** | **これまで参照していなかった。** Docker v1.8.0（2025-08）、Kubernetes v2.0.0（2026-05）、PostgreSQL 18 v1.0.0（2026-05）。`references/13` から参照 |
| IPA 非機能要求グレード | 2018 が本体の最新で変わらず。関連資料「重要情報を扱うシステムの要求策定ガイド」が **Ver.1.1（2026-09-11）** |
| **個人情報保護委員会ガイドライン（通則編）** | **令和 8 年 6 月 14 日改正が最新。** スキルは「改正法が段階施行」としか書いておらず、**ガイドライン自体が改正済み**であることを反映 |

### 追加

- **`06` に安全管理措置の 7 区分との対応表**（基本方針／規律の整備／組織的／人的／物理的／
  技術的／外的環境の把握）。**人的・物理的の 2 区分はコードから判定できない**ので
  「本評価の範囲外」と書き、未確認と混同させない、とした。
  **技術的安全管理措置だけを見て「安全管理措置を講じている」とは書かない**
- **`08` に要配慮個人情報の観点。** これを含む漏えいは**件数にかかわらず報告義務の対象**。
  葬儀・医療・福祉・法律相談のような業種では、サービスの性質上入り込む
- **`08` に一次情報の確認先**（ppc.go.jp の「法令・ガイドライン等」）
- **`13` に CIS Benchmarks への言及。** この資料は「AI で素早く作った構成で実際に空く穴」に
  絞っており網羅を狙っていない。網羅が要るときは製品の版に合った Benchmark を取る。
  **Level 2 まで全部を適用するものではない**（可用性や運用と衝突する項目がある）
- `06` の「参照している基準の版」の表に**確認元の列**を足した。**すべて一次情報で確認した版**と明記

### 追加: セキュリティ警告情報の組み込み

基準（何に照らすか）とは別に、**「今、何が起きているか」を評価のたびに見る情報源**を入れた。

| 情報源 | 役割 |
|---|---|
| **CISA KEV** | 実際に悪用が確認された脆弱性の公式カタログ。**CVSS ではなく「今、使われている」を優先度の入力にする** |
| JVN iPedia | 国内の脆弱性対策情報。依頼者に日本語の一次情報で説明できる |
| JPCERT/CC 注意喚起・IPA 重要なセキュリティ情報 | 国内で影響が大きいもの。**評価の最初と最後に見る** |
| GitHub Security Advisories | `npm audit` 等の元。修正版の有無 |

- `10` に 1-2 節「実際に悪用されているかで優先度を分ける」。KEV の JSON とスキャン結果を
  突き合わせるコマンドを置いた。**KEV 収録は「今すぐ」の根拠、それ以外の high は
  「到達性を確かめてから」**という区別を台帳に書く
- `04` の優先度に「悪用が確認されていれば上げる」
- `11` の再評価に「前回以降に出た注意喚起を見る」。**コードを 1 行も変えていなくても、
  前回「問題なし」だった版が今回は KEV 収録になっている**ことがある
- `06` に「警告情報の確認先」の表（基準の表とは別枠）
- 検査の「実在しうるドメイン」の許可リストに、公的な基準・警告情報の発行元だけを追加。
  **案件のドメインは引き続き捕まえる**ことを確かめてある

`standards-reviewed` を 2026-09-11 に更新。検査は 288 件（変わらず）。

---

## [2.9.0] — 2026-09-11

**2 件目の実案件（dry-run）で見つかったものを反映した回。** 対象は Next.js 16 + Supabase の
BtoB 業務システム。取材と実機確認は行わず、コードの読解だけで進めた。

### 実案件で見つかった、スキル側の穴

**スクリプトの穴が 5 つ、資料の穴が 3 つ。** 前回（2.2.0〜2.4.0）で 52 の枠組みに広げたにも
かかわらず、実案件に当てると初日でこれだけ出た。**題材はどれも人工で、実案件の癖を模していない。**

スクリプト:
- **バイナリファイルに grep が当たっていた。** 画面キャプチャの PNG が「HTML を直接流し込む」に
  並んだ。全 grep に `-I` を足した
- **JSX の `<select>` `<DeleteButton>` を SQL と誤検出していた。** SQL キーワードの直後に
  `from` / `into` / `set` を要求するようにした
- **業務上の役割名を付けた `require<役割>()` をガードとして読めなかった**（`requireUser` /
  `requireAdmin` しか語彙に無かった）。**ガードがあるのに無いと出る、逆向きの誤り。**
  `require[A-Z]...` の命名規約で拾うようにした
- **Next.js 16 の `proxy.ts`**（旧 `middleware.ts`）を 2c が見ていなかった
- 8 節のテーブル名抽出に `auth` / `https` / `public` / `your` のような英文の断片が混ざる（未修正）

資料:
- **更新ポリシーが列を制限しない問題**が 03 に無かった。`for update using (id = auth.uid())` は
  「自分の行なら全列を更新できる」で、認可の根拠（`role` / 所属）がそのテーブルにあれば
  **利用者が自分を管理者にできる**。今回の最優先指摘がこれだった
- **認証基盤のメタデータからロールを決める問題**が 02 A-2 に無かった。サインアップ要求に
  `role` を入れるだけで管理者になれる
- **`public` スキーマの関数は RPC として公開される**ことを、03 の迂回経路に明示していなかった

### 実案件の評価そのもの

- **設計は際立って良い**（RLS 全テーブル有効、`anon` 権限なし、Storage のフォルダ強制、
  Server Actions 全ガード、秘密情報の混入なし）。**それを 1 か所が崩している**、という構図
- 指摘が少ないのは、見るべき経路が少ないからではなく実装が良いから
- dry-run のため、実機確認が要る 9 件はすべて未確認事項に残した。**「確認範囲はコードのみ」を
  サマリの冒頭に書いた**

検査は 288 件（変わらず）。案件語を機密検査の語彙に追加した。

---

## [2.8.0] — 2026-09-11

**初めて別の実案件に当てる直前に見つかった穴。** 対象は Next.js 16 で、`route.ts` が 1 本も無く、
状態変更をすべて Server Actions（`'use server'`）で行っていた。

### 見つかった状態

- Server Actions のファイルは**偶然**拾えていた（`export async function` を数える汎用パターンに
  掛かっただけで、`'use server'` を見ていなかった）
- **`components/` と `lib/` のファイルがハンドラとして並んでいた**（偽陽性）。
  `server.ts` というファイル名を Fastify のエントリポイントとして入れていたが、
  `lib/supabase/server.ts` のようなクライアント生成の置き場も同じ名前になる
- ファイル単位の「定義 19 / ガード 7」では、**どの関数が素通しかまでは分からなかった**

### 追加

- **`'use server'` を明示的に拾う**（handler_files の 3b）
- **2d「Server Actions の関数ごとのガード」** — export した関数 1 つ 1 つについて、
  関数の先頭から次の export までにガードがあるかを出す。**ファイル単位では埋もれていた
  「ガードの無い関数」が名指しで出る**
- **`references/02-code-audit.md` に Server Actions の節** — `route.ts` と同じ重さで見る。
  取り違えやすい点を 3 つ書いた（引数は信頼できない／戻り値はクライアントへ渡る／
  ガードを呼んでいることと効いていることは別）。**「`route.ts` が無いから API が無い」とはならない**

### 修正

- `components/` `utils/` `hooks/` `types/` `tests/` をハンドラの収集から除外した。
  `lib/` も除外するが、**Phoenix の `lib/*_web/controllers/` は残す**
  （最初は一律に除外して Phoenix の検査が落ちた）
- awk に正規表現を `-v` で渡すとバックスラッシュが解釈されて壊れる。環境変数経由に変えた

検査は 284 → 288 件。

---

## [2.7.0] — 2026-09-08

2.6.0 で資料（13 / 14）は足したが、**そこに「見る」と書いた観点を機械的に拾う仕組みが
無かった。** 資料だけあって下拵えが無いと、毎回すべてを手で探すことになる。

### 追加

- **`audit_grep.sh` の 17 節「インフラの定義」**（0 節で該当したときだけ出す）
  - コンテナ: `USER` の指定が無い（root で動く）、`privileged`、`/var/run/docker.sock` の受け渡し、
    `ARG`/`ENV` に渡した秘密、`:latest`、`.dockerignore` の不在
  - クラウド資源: `0.0.0.0/0`、`public-read`、`Action: "*"`、`terraform.tfstate` の所在と git 履歴
  - Kubernetes: `kind: Secret`、`privileged`、`runAsUser: 0`、**`NetworkPolicy` の不在**
- **`audit_grep.sh` の 18 節「モバイルアプリ」**（同上）
  - 保存先を 2 つに分けて出す。**平文で残る置き場**（`AsyncStorage` / `UserDefaults` /
    `SharedPreferences`）と、**資格情報の置き場として意図されたもの**（`Keychain` /
    `SecureStore` / `EncryptedSharedPreferences`）
  - 通信（`usesCleartextTraffic` / `NSAllowsArbitraryLoads` / 証明書検証の回避）、
    端末との境界（ディープリンク、WebView、`exported="true"`）、権限、`debuggable`

**どちらも 0 節の判定で該当したときだけ出す。** 対象に無い技術の見出しを並べても、
「確認したつもり」を増やすだけになる。

### 修正

- **伏字が引用符付きの値しか消していなかった。** `ARG NPM_TOKEN=npm_xxxx` のように
  引用符で囲まれていない代入は、**値がそのまま出力に出ていた。**
  Dockerfile の `ARG` / `ENV` が典型で、IaC の題材を作るまで露呈しなかった。
  名前に KEY / SECRET / TOKEN / PASSWORD を含むものだけを対象に、引用符の無い代入も
  伏字にした（無関係な代入まで潰さないため）

検査は 268 → 284 件。**伏字の規則を外すと実際に落ちる**ことを確認している。

---

## [2.6.0] — 2026-09-08

2.5.0 で「意図的に見送った」と書いた 2 つを追加し、あわせて
**対象の構成に応じて読む資料を決める仕組み**を入れた。

### 追加

- **`references/13-infrastructure.md`** — インフラを自分たちのリポジトリで定義しているとき
  - コンテナ（root 実行、`ARG`/`ENV` に渡した秘密がイメージの履歴に残る、`.dockerignore` の不在、
    `:latest`）、クラウド資源（全開放、`*` 権限、暗号化とログ、**`terraform.tfstate` に平文で入る秘密**）、
    Kubernetes（`Secret` は base64 であって暗号化ではない、NetworkPolicy、RBAC）、サーバーレスの定義
  - **マネージド基盤に載せているだけなら不要**と冒頭に書いた。設定は管理画面にあり
    コードに現れないので、03 で実機を見るほうが確実になる
  - **「IaC が無いこと」自体は指摘ではない。** 指摘になるのは、誰が何を変えたか分からない場合
  - インフラの是正は「直すと壊れる」危険が高いので、**「先に調べること」を手順に書く**とした
- **`references/14-mobile.md`** — 評価対象にモバイルアプリが含まれるとき
  - OWASP MASVS 2.1 の 8 分類（Storage / Crypto / Auth / Network / Platform / Code /
    Resilience / Privacy）に沿って並べた
  - **アプリは配布物**という前提の違いを最初に置いた。「アプリの中にあるから見えない」は
    成り立たない。サーバー側で守れないものは守れない
  - **鍵が入っているだけでは指摘にしない。** Firebase の設定は公開前提で、守るのは
    サーバー側の規則になる。**その鍵で何ができるか**で判定を分ける表を置いた
  - **難読化・改竄検知は優先度を上げすぎない**とも書いた。時間稼ぎであって防御ではなく、
    単独で挙げると本当に危ないものが埋もれる
  - **通信の中継（端末に証明書を入れて覗く作業）は評価者が代行しない。**
    配布物の解析も、依頼者の許可を明示的に取ってから

### 追加: 構成の判定（`audit_grep.sh` の 0 節）

**対象に無い技術の資料を読ませないための仕組み。** 最初にこれを回すと、
この案件で追加で読む資料が名指しで出る。

判定するもの: インフラの定義 / モバイルアプリ / LLM の利用 / 計測・広告タグ /
XML の解析 / ロックファイルの有無。

**無い資料は読まない。** 対象に無い技術の観点を報告書に並べると、
**見たことになっていない項目が「確認済み」に見える。** これは未確認を隠すのと同じ害になる。

**逆に、判定はファイルの有無しか見ていない。** 管理画面で手作業で作った資源はコードに
現れないので、**「インフラの定義が無い＝設定を見なくてよい」ではない**とも書いた。

検査は 260 → 268 件（「有」と「無」の両方が正しく出ることを見る）。

---

## [2.5.0] — 2026-09-08

**網羅性を外部の基準と照合し、漏れていた観点を埋めた回。**
OWASP ASVS 5.0（17 章・約 350 項目）と CWE Top 25 (2025) に対して、
`references/02-code-audit.md` の観点を 1 章ずつ突き合わせた。

### 見つかった漏れ

**「乱数」「TLS」がスキル全体で 1 度も出てこなかった。**

- **ASVS V11（Cryptography）に相当する観点が無かった。** パスワードの保存（B-1）は
  あったが、**予測できる乱数**・暗号の使い方・鍵の導出が抜けていた
- **ASVS V12（Secure Communication）に相当する観点が無かった。**
  とくに**証明書の検証を切る設定**を見ていなかった
- ASVS V5（File Handling）のうち、**展開や変換で膨らむ形**（zip 爆弾）と **XXE** が抜けていた

### 追加

- **`02` の N 節「乱数と暗号」**
  - **`Math.random()` は暗号用ではない。** 再設定トークン・セッション ID・招待コード・
    メール確認リンクに使われていれば、それだけで指摘になる
  - **画面の見た目に使う乱数は問題ない。** 用途を追わずに一律で挙げると、指摘の数だけが増える
  - パスワードハッシュの方式を 4 段階で判定する表。認証基盤に任せている場合は
    「未確認」と「基盤が担っている」を書き分ける
  - 自前で暗号を組んでいる場合のみ見る項目（ECB、固定 IV、改竄検知）
- **`02` の O 節「通信の保護」**
  - `rejectUnauthorized: false` / `verify=False` / `InsecureSkipVerify` /
    `CURLOPT_SSL_VERIFYPEER` / `NODE_TLS_REJECT_UNAUTHORIZED`
  - **「動かないからとりあえず無効化」がそのまま残る。** 環境で分岐していても、
    その分岐が `NODE_ENV` に依存していれば K 節（fail-open）と同じ問題になる
- **`07` の 6-2「XXE」と、展開・変換によるリソース枯渇**
  - XML を受け取る構成だけが対象（SVG、SOAP、RSS、Office 文書、SAML）。
    **JSON しか受け取らないなら不要**と明記した
- **`06` に ASVS への言及** — Top 10 が「起きがちな 10 種類」なのに対し ASVS は検証要件。
  **報告書に載せるには細かすぎる**ので、使うのは「取引先が水準で要求してきたとき」と
  「評価者が自分の監査の抜けを自己点検するとき」に限る、と書いた。
  02 の各節と ASVS の章の対応も載せた
- **`audit_grep.sh` に 13〜15 節**（乱数と暗号 / 通信の保護 / XML の解析）

検査は 256 → 260 件。乱数の検出パターンを壊すと落ちることを確認している。

---

## [2.4.0] — 2026-09-08

題材を **52 の枠組み**に広げ、あわせて**現実に近い構成**の題材を 1 つ作った。
2.3.0 までの題材はどれも「ガードあり 1 本・なし 1 本」の最小構成で、
**実際に穴が空く形を模していなかった。**

### 追加した枠組み（14）

**サーバーレスを重点的に足した。** AI で素早く作ったアプリでよく使われる割に、
これまで 1 つも題材が無かった。

| 種別 | 枠組み |
|---|---|
| サーバーレス | AWS Lambda、Firebase Functions、**Supabase Edge Functions**、Azure Functions |
| CMS | WordPress（REST API） |
| JS | AdonisJS、Elysia (Bun)、SolidStart、Qwik |
| JVM / .NET | Micronaut、Razor Pages、Play (Scala) |
| その他 | Vapor (Swift)、Rocket (Rust) |

これらは**ルートの表し方が根本的に違う**。関数そのものが 1 つのルートになる
（`exports.handler` / `Deno.serve` / `functions.https.onRequest`）、設定ファイルで
経路と権限を宣言する（WordPress の `permission_callback`、Play の `conf/routes`）、
命名規約で決まる（Qwik の `onGet`、Razor Pages の `OnGet`）。

### 追加: 現実に近い構成の題材

`tests/fixtures/repo-realistic/` に、次の 4 つを模した題材を置いた。

1. **認可の層が 2 つある**（ミドルウェア + ルートごとのガード）
2. **ミドルウェアの対象から外れたルートがある**（`matcher` の書き方による穴）
3. **深い入れ子のルート**
4. **ルートを動的に組み立てる箇所**

正しく作られている側（署名を検証する Webhook、2 層でガードするルート）も置いてある。

### 追加: 2c「ミドルウェアの対象範囲」

**対象範囲の指定は、書き方しだいで簡単に穴が空く。** `matcher` や除外指定を表示し、
ルート一覧と突き合わせるよう促す節を足した。`references/02-code-audit.md` の A-5 にも、
よくある 3 つの穴（前方一致から外れる／除外が広すぎる／後から足したパスが入っていない）を書いた。

**対象外であること自体は問題ではない。** Webhook の受け口は認証を通らないのが正しい。
問題は、**対象外なのにガードも無い**こと。

### 修正

- **ユーティリティをハンドラとして数えていた。** `./lib` をディレクトリ収集の対象に
  していたため、`lib/db.ts` のような置き場が「ガードの無いハンドラ」として並んでいた。
  **偽陽性はノイズになり、本当に危ないものを埋もれさせる。** Phoenix の
  `lib/*/controllers` はファイル名で拾えるので、`./lib` は外した

検査は 203 → 256 件。

---

## [2.3.0] — 2026-09-07

題材を **37 の枠組み**に広げた。2.2.0 で 12 に増やしたときは 10 で穴が出たが、
今回さらに 24 を足したところ、**13 でハンドラを 1 つも見つけられていなかった。**

### 対応した枠組み

| 言語 | 枠組み |
|---|---|
| JS / TS | Next.js（App / Pages）、Nuxt、SvelteKit、Astro、Remix、NestJS、Express、Koa、Hono、Fastify、tRPC、GraphQL、Cloudflare Workers、Deno Fresh、Angular |
| Python | Django、DRF、FastAPI、Flask、Tornado |
| Ruby | Rails、Sinatra |
| PHP | Laravel、Symfony、Slim |
| Go | net/http、Gin、Echo、Fiber、Chi |
| JVM | Spring、Quarkus（JAX-RS）、Ktor |
| .NET | ASP.NET Core MVC、Minimal API |
| Rust | Actix、Axum |
| Elixir | Phoenix |

### 変更

- **ハンドラの集め方を 4 経路にした** — ファイル名 / ディレクトリ / ルート登録 /
  ハンドラの実装シグネチャ。**同じ枠組みでも登録と実装が別ファイルに分かれる**ため
  （Go と Ktor が典型）、どれか 1 つでは足りない
- **ガードが「呼び出し」の形をしていない枠組みに対応した**
  - Actix は引数の型（`user: AuthenticatedUser`）、Ktor はブロックで囲む形
    （`authenticate("...") { }`）、tRPC は手続きの種類（`protectedProcedure`）
  - **関数呼び出しだけを探すと見落とす。** これは「ガードがあるのに無いと読む」誤りで、
    逆向きなので危ない
- **SQL の文字列連結の検出を、引用符の連続と埋め込み構文に対応させた**
  - `where email = '" + email + "'` のように SQL 内に引用符があると、
    終端を 1 文字に決め打ちしたパターンでは拾えなかった
  - Rust の `format!` と Python の `.format` が使う `{}` 埋め込みを追加。
    13 言語すべてで検出することを確かめた
- **クラスベースのハンドラを数えられるようにした**（DRF の ViewSet、Tornado の
  RequestHandler、JAX-RS の Resource）
- **単一の入口で自前にルーティングする構成**（Cloudflare Workers の `fetch`、
  Lambda のハンドラ 1 本）は、ハンドラの数を機械的に数えられない。
  **数えられないことを資料に書き、検査でも免除した。** 数えられないのに
  「全ルートを確認した」と書くほうが危ない

### 修正

- **`grep -c` の戻り値の扱いが誤っていた。** `grep -c` はマッチ 0 件でも `0` を出力した
  うえで終了コード 1 を返すため、`|| echo 0` を付けると `0\n0` の 2 行になり、
  数値比較が構文エラーで落ちていた。**ガードが 1 つも無いファイルでだけ起きる**ため、
  題材を増やすまで露呈しなかった

### 検査

**37 の枠組みそれぞれで中核の 3 つ**を検査する（203 件）。
SQL 連結のパターンを壊すと 6 件が落ちることを確認したうえで入れている。

---

## [2.2.0] — 2026-09-07

**題材を 12 の枠組みに広げたところ、中核の検出が 10 枠組みで丸ごと効いていなかった。**
2.1.0 で「題材を 1 つ増やすたびに穴が出ている」と書いたが、実際はもっと大きい穴だった。

### 見つかった状態

`audit_grep.sh` の中核は「ハンドラを列挙し、認可ガードの無いものを名指しする」こと。
これが **Next.js App Router / Rails / Django でしか動いていなかった。**

Nuxt / Laravel / Go / Express / FastAPI / Flask / Spring / ASP.NET / SvelteKit /
Next.js Pages Router のいずれでも、2 節（ハンドラ × 認可ガード）が「検出なし」になっていた。
**ガードが無いのではなく、ハンドラを 1 つも見つけられていなかった。**

これは「検出できない」より悪い。**空欄が出ないので、見落としに気づけない。**

### 変更

- **ハンドラの集め方を 3 通りに整理した**
  - **ファイル名で決まる**（Next.js App / SvelteKit / Rails / Django / Spring / ASP.NET / NestJS）
  - **ディレクトリで決まる**（Next.js Pages / Nuxt / Laravel / Symfony）
  - **コード中の登録で決まる**（Express / Flask / FastAPI / Go / Laravel のルート定義）
  - **Go はルート登録と実装が別ファイルに分かれる**ため、ハンドラのシグネチャ
    （`http.ResponseWriter` / `gin.Context` / `echo.Context` / `*fiber.Ctx`）でも拾う
- **認可ガードの語彙を枠組み横断にした**
  - Nuxt の `requireUserSession`、SvelteKit の `locals.user`、Laravel の `->middleware` /
    `Gate::`、Spring の `@PreAuthorize`、ASP.NET の `[Authorize]`、FastAPI の `Depends(` など
  - **語彙が足りないと、ガードがあるのに「無い」と読む。** 逆向きの誤りなので、より危ない
- **2b「ルート登録の一覧」を追加** — `app.get('/x', requireAuth, handler)` のように
  登録の行で認可を挟む書き方を見る。Go の `http.HandleFunc` もここに出る
- **危険な関数の検出を言語横断にした**
  - SQL の文字列連結を、`+`（JS/Go/Java/C#）`.`（PHP）`%` と `.format`（Python）
    `||` をまとめて 1 つのパターンで見る。**言語別に書くと必ず取りこぼす**
  - コマンド実行も `exec.Command`（Go）`shell_exec` / `passthru`（PHP）
    `Runtime.getRuntime().exec` / `ProcessBuilder`（Java）`Process.Start`（C#）を追加
  - HTML の直接出力に `@Html.Raw`（ASP.NET）`|safe`（Jinja）`html_safe`（Rails）
    `mark_safe`（Django）`{{{ }}}`（Handlebars）を追加
- **クライアントに出る環境変数の接頭辞**を 13 種に（Nuxt / Astro / Gatsby / Vue CLI /
  Angular / Svelte / Remix / Storybook などを追加）
- **共通レイアウトの探索先**を 20 箇所に（Blade / ERB / Jinja / Thymeleaf / Nuxt の
  `app.vue` と `layouts/default.vue` などを追加）
- **テーブル名の抽出**を、ORM の書き方に加えて **SQL 文字列の from / join / into 句**からも拾うようにした
  （Go / Java / C# のように ORM を使わない構成のため）
- `references/02-code-audit.md` に、**ハンドラの置き方 3 通りの表**と、
  **枠組みごとの認可の現れ方の表**を追加

### 検査

**12 の枠組みそれぞれについて、中核の 3 つを検査する**ようにした（125 件）。

- ハンドラを見つけられるか
- ガードを読み取れるか
- **ガードの無いハンドラを名指しできるか**（1 ファイルに全ハンドラが入る枠組みでは
  「定義 N / ガード M」の差で示す）

ガードの語彙を 1 つ壊すと実際に落ちることを確認したうえで入れている。

---

## [2.1.0] — 2026-09-07

2.0.0 で「実案件で使っていない」「題材が 1 つの枠組みだけ」として残していた課題への対処。
**手元の事例と突き合わせれば検証できるもの**が残っていた。

### 変更

- **`--frameworks full` の 2 枚を、実案件で使った実物から作り直した**
  - **きっかけ**: 雛形は外部の基準から起こしたもので、実案件の台帳と突き合わせていなかった。
    比べたところ、個人情報のシートは雛形 7 項目に対し**実物は 11 区分 51 項目**、
    IPA のシートは雛形が大項目 6 行に対し**実物は中項目 29 行**と、骨格から違っていた
  - 実物の項目を汎用化して取り込んだ（製品名・鍵の呼び名を外し、
    「行レベルの権限制御」「公開鍵」のような言い方に置き換えた）
  - 2.0.0 で追加した観点も足した（トークンの署名検証、戻り先 URL の完全一致、
    権限制御を迂回する経路、Webhook の真正性、例外時にどちらへ倒れるか、従量課金の上限）
  - IPA のシートは、モデルシステム対比の結合セルも実物に合わせた

### 追加

- **`--api` で OWASP API Security Top 10 (2023) のシートを作れるようにした**
  - API が主体の構成のときだけ使う。一般の Top 10 と重なるため、両方は並べない
  - 一般の Top 10 では拾いにくい **API3（プロパティ単位の認可）と API9（旧版 API の放置）**
    のために当てはめる価値がある、と資料にも書いた
- **基準の棚卸しを仕組みにした**
  - `references/06-frameworks.md` に**参照している基準の版と最終確認日**を置いた
  - `tests/run.sh` がその日付を読み、**1 年以上前なら知らせる**。
    ただし**失敗にはしない**。日付が過ぎただけで検査が赤くなると、本当の失敗が埋もれる
- **`audit_grep.sh` の題材を Rails と Django にも広げた**（検査は 76 → 84 件）

### 修正

題材を広げたことで、**Next.js 系だけを見ていたときには気づけなかった穴が 3 つ**出た。

- **Django のビューがハンドラ一覧に出ていなかった。** 1 節で件数を数えているのに、
  2 節の一覧はファイル名が `route.ts` / `*_controller.rb` のものしか拾っていなかった。
  **数えているのに見ていない**状態だった
- **Python の文字列フォーマットで組み立てた SQL を検出していなかった。**
  `execute(` を伴わない行があるため、SQL らしさで拾うようにした
- **1 ファイルに複数のハンドラがある枠組みで、ガード漏れが埋もれていた。**
  ガードが 1 つでも見つかればガードありと表示され、同じファイルの他のハンドラが
  素通しでも気づけない。**「定義 N / ガード M」を併記**して、差があれば分かるようにした
- `tests/run.sh` で、**変数展開の直後にマルチバイト文字が来ると bash 3.2 が
  変数名の一部と誤認する**箇所があった（`$STD）` が `STD）` として解釈され停止）。
  波括弧で明示し、同種の箇所を機械的に洗って潰した

---

## [2.0.0] — 2026-09-07

**外部の基準と脅威動向を調べ直し、抜けていた観点をまとめて入れた回。**
きっかけは、スキルが参照している OWASP Top 10 が 2021 版のままだと気づいたこと。
調べたところ、他にも前提が古い箇所と、主対象（AI で素早く作ったアプリ）に直結するのに
扱っていない観点が見つかった。

### 変更（利用者に影響するもの）

- **OWASP Top 10 の既定を 2025 版にした。** 2021 版は `--owasp 2021` で引き続き作れる
  - 2025 版では **SSRF が A01 に統合**され、**A03 がサプライチェーン全体**に、
    **A10 が例外処理（fail-open を含む）**になった。**A02（設定不備）が 5 位から 2 位**へ
  - スキルが独自に重視してきた fail-open とサプライチェーンが、正式カテゴリに昇格した形になる
  - **どちらの版で書いたかを明記する**よう `references/06-frameworks.md` に書いた。
    版を書かない適合表は、読み手が何と比べているか分からない

### 追加

- **`references/12-ai-features.md`** — 評価対象のアプリ自身が LLM を呼んでいるとき
  - プロンプトインジェクション（直接／間接）、出力の取り扱い、**権限の広さ**、
    文脈に入る秘密、費用と濫用、MCP・外部ツールサーバー
  - **「AI で開発した」と「AI を動かしている」は別物**として整理した。要るのは後者だけ
  - インジェクションは原理的に塞げないので、**「破られたときに何ができるか」**を見る立て付けにした
- **`references/06-frameworks.md` に OWASP API Security Top 10** — API が主体の構成向け。
  一般の Top 10 では拾いにくい API3（プロパティ単位の認可）と API9（旧版 API の放置）のために使う
- **`references/02-code-audit.md` に 3 節**
  - **B-4 トークンの検証** — 署名を見ずに復号しているだけの実装、`alg` の固定、`aud`/`iss`/`exp`
  - **B-5 OAuth / OIDC** — `state` の照合、PKCE、**`redirect_uri` の完全一致**
  - **M Webhook と外部からの通知** — 署名検証。**シークレット未設定のときに検証を飛ばす形**が
    最も多い穴になる。定数時間比較、タイムスタンプ、冪等性、経路の照合
  - **K-2 例外が起きたときにどちらへ倒れるか** — 認可・署名検証の周りでの握りつぶし
- **`references/02-code-audit.md` の A-5 に、枠組み自体が破られる場合**
  - ミドルウェアで認可する構成は、その仕組みに穴が出た時点で全ルートが同時に開く。
    実例として Next.js の CVE-2025-29927（ヘッダ 1 つでミドルウェアを迂回）を挙げた
  - **是正は版上げとルート単位のガードの二段**にする、と書いた。版上げだけでは次に同じことが起きる
- **`references/03-runtime-verification.md` に、ポリシーの中身と迂回経路**
  - **有効なだけでは足りない。** `USING (true)` の素通し、クライアント由来の値での絞り込み
  - **迂回する 3 経路** — 定義者権限の関数、呼び出し元の権限で動かないビュー、特権鍵
  - この 3 つを確かめずに「データ層の防御は機能している」と書かない、とした
- **`references/07-web-vulnerabilities.md` の 7 節にキャッシュの 3 分類**
  - 漏えい／**デセプション**／ポイズニングを分けた。混ぜると是正の指示を間違える
  - デセプションは枠組みとホスティングの組み合わせで起きるため、**コードを読むだけでは見つからない**
- **`references/08-privacy-compliance.md` に、制度が変わる時期であること**
  - 課徴金、こどもの個人情報、生体情報、外部送信規律の強化
  - **「改正で違反になる」とは書かない。** 該当しうる項目を扱っている事実と、
    施行時期を踏まえた法務確認の要否までにする
- **`references/10-dependencies.md` に幻覚パッケージと枠組み本体の脆弱性**
  - 生成コードの 2 割前後が存在しないパッケージを参照するという調査がある。
    **AI で素早く作ったアプリは 1 回で依存が一気に増える**ため、この観点の比重が高い
- **`scripts/audit_grep.sh` に 4 節**（10 トークン検証／11 Webhook／12 例外の握りつぶし／13 LLM 利用）
- **日本語の資料に想定外の文字体系が混ざっていないかの検査**
  - 生成の過程でキリル文字やハングルが 1 語だけ紛れ込むことが実際に 2 回起きた。
    校正で見落としやすく、読み手には意味が取れない

検査は 71 → 76 件。

---

## [1.5.0] — 2026-09-07

**誤検出しないことの検査**を足した回。

### 追加

- **発火台に「正しく作られている側」（`/clean`）** — 同意を取るまで第三者へ送らず、
  `HttpOnly` を付け、CSP に `unsafe-inline` を持たず、セキュリティヘッダを揃えたページ
- **`recon.sh` と `browser_probe.mjs` の誤検出の検査**（検査は 62 → 71 件）
  - **きっかけ**: それまでの検査はすべて「穴を検出できるか」だけを見ていた。
    **誤検出するツールは、指摘の山に埋もれて本当に危ないものを隠す。** スキルが
    「指摘を数で競わない」と書いている以上、道具の側も数を水増ししてはいけない
  - `HttpOnly` の判定を反転させると誤検出の検査が落ちることを確認したうえで入れている

---

## [1.4.0] — 2026-09-07

1.3.0 で残していた課題のうち、今できる 2 つに対処した回。

### 追加

- **`templates/` — 依頼者に渡す文面を置く場所**（スキルの 4 つ目の構成要素）
  - `templates/client-browser-checklist.md` — 認証が要るブラウザ確認のお願い
  - **きっかけ**: `references/09` の 2・3・6・7 節は依頼者にやってもらう前提なのに、
    渡す形になっていなかった。毎回、資料から手順を抜き書きすることになる
  - **判断を求めない形にした。** 「安全かどうか」ではなく「何が見えたか」を書いてもらう。
    依頼者に判断を求めると、答えが解釈で汚れて事実として使えなくなる
  - 開発者ツールの開き方から書き、送信ボタンを押さないよう明記した。
    「よく分からなかった」もそのまま書いてもらう（未確認と分かるほうが安全）
- **既知タグのラベル付けの検査**（検査は 61 → 62 件）
  - **きっかけ**: 発火台はローカルのため `googletagmanager.com` というホスト名を作れず、
    1.3.0 では検査できていなかった
  - `browser_probe.mjs` の実行部分を `main()` に入れ、直接実行時のみ動くようにしたうえで
    `TAGS` を export した。**import しても副作用が起きない**ので、部品だけを検査できる
  - 12 種のラベル付けに加え、`notgoogle.example.com` のような紛らわしいホストを
    誤判定しないことも見ている

### 変更

- 検査の孤児チェックと参照先チェックが `templates/` も見るようになった

---

## [1.3.0] — 2026-09-07

1.2.0 で追加したブラウザ確認を、実際に動かして確かめた回。あわせて `recon.sh` の
動作検査（1.1.0 から残っていた課題）も同じ仕組みで塞いだ。

### 追加

- **発火台（`tests/fixtures/site/`）** — わざと穴のあるページを `localhost` で配信する
  小さなサーバー。同意前に第三者へ送信し、`HttpOnly` の無い Cookie を発行し、
  `localStorage` にトークンを置き、CSP に `unsafe-inline` があり、クライアントに鍵が載っている
  - **第三者は `127.0.0.1` で模す。** ページは `localhost` で開くため、ブラウザから見て
    別ホストになる。これで外部へ一切出ずに、検出の中身まで検査できる
- **`recon.sh` と `browser_probe.mjs` の実地検査**（検査は 45 → 61 件）
  - **いちばん大事なのは「値を出力しない」検査。** Cookie と保存領域の値を出すよう改変すると
    実際に落ちることを確認したうえで入れている。鍵の伏字も同じ

### 修正

- **`browser_probe.mjs` が「送信を試みた」と「実際に届いた」を区別していなかった。**
  CSP でブロックされたリクエストも同じ数に入っていた。実地で当てて気づいたもので、
  区別しないと報告に「同意前に送信している」と書けない場合を書いてしまう
- **`recon.sh` がポート付き URL のとき、自サイトを第三者として数えていた。**
  `https://example.com:8443` のような構成で誤判定する

### 分かっていること

`--frameworks full` で増えた 2 枚（個人情報・IPA）は、実物から起こしてはあるが
**まだ実案件で使っていない**。次の案件で当ててから直すべきところが出るはず。

---

## [1.2.0] — 2026-09-07

1.1.0 の時点で「分かっている課題」として残していた 3 つに対処した回。
きっかけは **1 件目の実案件（2026-08）**の振り返りで、初回の評価で扱いきれなかった観点。

### 追加

- **`references/09-browser-verification.md` — ブラウザでの確認**
  - **きっかけ**: 実案件では計測タグの有無を HTML の grep で判定していたが、
    **タグがあることと、同意前に発火することは別**で、静的な確認では判定できない。
    法令面の指摘を書くのに、事実の裏付けが足りていなかった
  - curl で見えるものとの境界を表で示し、同意前の第三者送信・保存領域・CSP の実効性・
    権限の境界・戻るボタンでの再表示など 7 項目を扱う
  - **認証が要る確認は自動化していない。** 評価者が認証情報を扱わないという方針と、
    状態を変えないという方針の両方に触れるため、依頼者へ手順を渡す形にした
- **`scripts/browser_probe.mjs` — 上記のうち認証が要らない範囲の自動化**（Playwright）
  - Cookie を持たない素の訪問で、同意バナーに触れずに第三者への送信を数える
  - Cookie の属性、保存領域のキー名（値は取得しない）、CSP の `unsafe-inline`、
    キャッシュヘッダを取る。GET のみで、フォームの送信もクリックもしない
- **`references/10-dependencies.md` — 依存関係とサプライチェーン**
  - **きっかけ**: 実案件で `npm audit` の指摘が挙がった割に、
    `02-code-audit.md` の H 節は 3 行しかなかった
  - 件数ではなく**到達性**と**検知の仕組み**を見る方針にした。あわせて、
    インストール時に走るスクリプト・CI の Action の版固定・SRI といった、
    「他人のコードが本番に入る経路」を扱う
- **`references/11-reassessment.md` — 再評価（2 回目以降）**
  - **きっかけ**: 初回の評価を終えた時点で、次に同じ対象を見るときの手順が無かった。
    前回の台帳を見ずに洗い直すと ID が振り直され、見送りと判断したものがまた挙がる
  - 前回の指摘の突き合わせを 4 値（解消／未着手／部分対応／再発）にし、
    「解消」には必ず根拠を求める形にした。**依頼者の申告は根拠にならない**
  - ID は再利用せず、解消したものはクローズとして台帳に残す

### 変更

- `SKILL.md` の参照ファイル表を、**必ず読むもの（01〜05）と必要なときだけ開くもの（06 以降）**に
  分けた。資料が 11 本に増えたため、全部読むものではないことを明示する必要が出た
- `02-code-audit.md` の H 節に、件数をそのまま重大さとして書かない旨と 10 への導線を追加
- `03-runtime-verification.md` に「curl で見えないところ」を追加し、09 への導線を通した
- 検査を 40 件から 45 件へ（`.mjs` の実行権限・構文、`browser_probe` の引数処理と依存不在時の案内）

---

## [1.1.0] — 2026-09-07

このリポジトリを作り、スキルを単体の `.skill` から保守できる形に移した回。
きっかけは **1 件目の実案件（2026-08）**で、その成果物と突き合わせて分かったことを反映した。

### 追加

- **台帳の枠組みシートを 3 構成から選べるようにした**（`make_register.py --frameworks`）
  - `none` 6 枚 ／ `owasp` 7 枚（既定・従来と同じ） ／ `full` 9 枚
  - **きっかけ**: 実案件で作った台帳は 9 枚だったが、スクリプトは 7 枚しか作れなかった。
    生成に使ったスクリプトは失われていたため、実物の構成から起こし直した。差分は
    `references/06-frameworks.md` が扱う 3 つの当てはめ（個人情報・OWASP・IPA 非機能要求
    グレード）のうち、OWASP しかシート化されていなかったこと
- **`references/04-findings-register.md` に「何を 1 件と数えるかを決めて、書いておく」節**
  - **きっかけ**: 実案件でサマリ・修正指示書・台帳の 3 か所の件数が食い違った。原因は
    数え方の定義が揃っていないことで、どれも文脈上は正しかった。数字を 1 つに丸めるのではなく
    内訳を書く、という指針にした
- **検査（`tests/run.sh`・40 件）とビルド（`build/build.sh`）**
  - 機密性の検査が毎回走る。案件固有語の混入、例示以外のドメイン、`scan_secrets.sh` の自己検査

### 修正

- **`scan_secrets.sh` の検出結果が読めなかった**
  - 絶対パスのまま検索していたため、出力の先頭が長いパスで埋まり、検出内容が切り詰めの外へ
    押し出されていた。さらに `cut -c` はロケールによってバイト単位で動くため、日本語が途中で
    切れて文字化けしていた。**混入検査そのものが機能していなかった**
- **`audit_grep.sh` が同じ行を二重に出していた**（4 節の 2 本の grep が同じ行に当たる）
- **`make_register.py` が `openpyxl` 不在時に素の ImportError で落ちていた**。あわせて実行権限を
  付けた（`SKILL.md` は直接実行する形で書いているのに 644 のままだった）
- **`references/05-remediation-plan.md` に案件リポジトリ由来の相対リンクが残っていた**
- 配布物のパーミッションを 644 / 755 に揃えた（`.skill` 内が 600 / 700 で記録されていた）

### 事例側の資料を直したもの

1 件目の実案件の成果物を適用事例として取り込む際に、件数の食い違いを解消した。

| 出典 | 直す前 | 直した後 |
|---|---|---|
| トップ README | 28 件・人手 25.1h | 29 件・人手 25.4h（台帳の実データに一致） |
| remediation/README | 30 件・人手 25.9h | 同じ。ただし「実施する作業の数」だと明記 |
| 台帳（xlsx） | 但し書き無し | 数え方の但し書きを追加 |

---

## [1.0.0] — 2026-08-14

初版。**1 件目の実案件（2026-08）**の評価を通して形になったもの。`.skill` として単体で存在していた。

- `SKILL.md` — 進め方と、判断に迷ったときの拠り所 6 つ
- `references/` 8 本 — 取材・コード監査・実機確認・指摘台帳・修正指示書・枠組みへの当てはめ・
  アプリケーション層の脆弱性・プライバシーと法令遵守
- `scripts/` 4 本 — `recon.sh` / `audit_grep.sh` / `make_register.py` / `scan_secrets.sh`

このリポジトリの最初のコミットが、この版をそのまま取り込んだもの。
