# セキュリティの報告

## 受け付けるもの

このリポジトリの**スクリプト**（`skill/scripts/`、`build/`、`tests/` の実行コード）の脆弱性。
これらは、評価者の端末で、信頼できない他人のリポジトリやサイトを入力として動く。
例: 細工したリポジトリやページで、評価者の端末でコマンドが実行される、秘密情報が出力や外部へ漏れる、
対象の状態を変える操作が走る。

資料（`skill/references/`）の誤りで、評価の結論を誤らせるものも受け付ける。

## 受け付けないもの

- `tests/fixtures/` の題材。**わざと脆弱に作ってある**
- このスキルで評価した、依頼者のシステムの脆弱性。報告先は、そのシステムの運営者になる

## 報告のしかた

GitHub の **Security → Report a vulnerability**（非公開の報告）から送ってほしい。
公開の Issue には書かないでほしい。
