# 検査の題材と検査のコードには、わざと鍵らしい形の架空の値（DUMMY・FIXTURE を含むもの）を置いている。
# 本物の鍵ではないので、secret scanning の対象から外す。外すのは架空の値を置いているファイルだけにし、
# tests/ のほかのファイルや skill/・README などは対象のまま。
paths-ignore:
  - "tests/fixtures/**"
  - "tests/run.sh"
