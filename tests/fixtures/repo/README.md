# テスト用のダミーリポジトリ

`scripts/audit_grep.sh` が期待どおり検出するかを確かめるためだけのもの。
動くアプリではない。ここに置いてある鍵らしき文字列はすべて架空で、
実在のサービスに対して有効なものは 1 つも含まれていない。

`.env.local` はここには置いていない。`.gitignore` に `.env*` があるため
コミットできず、置いても CI では消える。`tests/run.sh` が実行時に作る。
